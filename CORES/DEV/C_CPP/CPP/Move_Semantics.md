# Move Semantics
instead of copying data and give ownership to another data
you can use Move semantics so you can use rvalue reference
to pass the temporary object

use std::move


Too much too fast i will revisit later
